{
  "name": "krishi-prabandh-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "dev": "uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.1",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "description": "A production-style FastAPI backend serving the KrishiPrabandh agricultural scheme processing platform. Uses a JSON flat-file as its data store for portability.",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
