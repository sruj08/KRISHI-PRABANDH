# API & server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.20

# Config & validation
pydantic>=2.10.0
pydantic-settings>=2.6.0
email-validator>=2.2.0

# Database & auth
supabase>=2.10.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# HTTP & testing
httpx>=0.27.0
pytest>=8.3.0

# OCR & image processing (services/ocr_engine)
opencv-python-headless>=4.10.0.84
pillow>=10.4.0
pytesseract>=0.3.13
# EasyOCR → python-bidi: 0.6.9 has no cp313 Windows wheel (sdist needs Rust+MSVC).
# Pin a 0.6.6–0.6.7 release so pip installs a binary wheel on Python 3.13 / win_amd64.
python-bidi>=0.6.6,<0.6.8
easyocr>=1.7.2
numpy>=1.26.4,<3
pymupdf>=1.24.14
dateparser>=1.2.0
